apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kdiff-snapshots.fullname" . }}-test-connection"
  labels:
    {{- include "kdiff-snapshots.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kdiff-snapshots.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
